Upload the contents of this folder to the ROOT of the GitHub Pages repository.

Required:
manifest.json
catalog/movie/mcu_complete_release_order.json

The single catalog is "MCU Complete — Release Order" and its data contains both
movies and TV entries in release order.
