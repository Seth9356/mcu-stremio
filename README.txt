# MCU Complete — Release Order

Static Stremio addon files.

Files:
- `manifest.json`
- `mcu_release_order_tv.json`
- `catalogs.json`

The supplied TV catalog contains the MCU TV-season entries in release-order positions.
The manifest exposes separate `movie` and `series` catalogs because Stremio catalogs are typed.
